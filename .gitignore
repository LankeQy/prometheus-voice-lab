# 虚拟环境文件夹
# 这是项目专属的Python库，体积巨大且每个人的系统都不同，绝对不能上传。
/venv/
.venv/
env/
.env/

# PyCharm IDE 配置文件
# 这是你个人的IDE设置，与项目代码无关。
/.idea/

# Python 缓存文件
# 编译后的pyc文件和缓存目录，会自动生成。
__pycache__/
*.pyc
*.pyo
*.pyd

# 操作系统生成的文件
# 例如 macOS 的 .DS_Store 或 Windows 的 Thumbs.db
.DS_Store
Thumbs.db

# 敏感信息文件
# 虽然我们现在没有，但这是一个好习惯，提前把 .env 文件加入忽略列表。
.env
.env.*
!.env.example

# 本地下载的模型文件夹 (如果我们采用手动下载方案)
# 模型文件通常非常大，不应该存放在Git仓库中。
/models/

# 其他临时文件或日志文件
*.log
*.tmp
*.swp